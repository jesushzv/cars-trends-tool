# Phase 0: Minimal dependencies
pytest==7.4.3
fastapi==0.104.1
uvicorn==0.24.0
httpx==0.25.1  # Required for TestClient

# Phase 1: Web scraping
requests==2.31.0
beautifulsoup4==4.12.2
# Note: Using built-in html.parser (no lxml needed)

# Phase 2: Database
sqlalchemy==2.0.35  # Latest version with Python 3.13 support
psycopg2-binary==2.9.10  # PostgreSQL adapter (Phase 17)

# Phase 11: Facebook Marketplace (JavaScript rendering)
playwright==1.49.1  # Latest version with Python 3.13 support

# Phase 14: Scheduling
apscheduler==3.10.4  # Job scheduling for automated scraping

# Phase 16: Authentication
python-jose[cryptography]==3.3.0  # JWT token handling
passlib[bcrypt]==1.7.4  # Password hashing
python-multipart==0.0.6  # Form data parsing

# Phase 19: CI/CD - Code Quality
flake8==7.1.1  # Python linting
black==24.10.0  # Code formatting
isort==5.13.2  # Import sorting
bandit==1.8.0  # Security scanning
safety==3.2.11  # Dependency vulnerability scanning
pytest-asyncio==0.24.0  # Async test support
pytest-timeout==2.3.1  # Test timeout handling

